"use client";

/**
 * Theme switching was removed — the site now renders the single Classic theme.
 * This component is a no-op kept for backward compatibility with any header
 * that still imports it. Rendering it produces nothing.
 */
export default function ThemeToggle() {
  return null;
}
